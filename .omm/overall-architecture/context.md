# Context

| Resource | Purpose |
|---------|---------|
| `docs/source/development/architecture.rst` | Full V1 architecture (layers, data flow, migration notes) |
| `docs/source/architecture/overview.rst` | English overview for readers |
| `habit/cli.py` | Subcommand registration only (thin shell) |
| `habit/core/habitat_analysis/habitat_analysis.py` | `HabitatAnalysis` deep module (`fit` / `predict`, `_PIPELINE_RECIPES`) |

Regenerate this tree when major refactors land; nested elements can be added under this folder.
