:orphan:

Rigor
=====

The Habitat Guide page for provenance and methods fingerprints was removed
from the sidebar. RunManifest and ``HabitatSpec.fingerprint()`` are documented
in :doc:`../development/architecture`.

Runnable demo (synthetic cohort)::

    python docs/source/examples/scripts/provenance_methods_demo.py

It is kept as a bookmark-stable URL and is intentionally **not** in the
Examples ``toctree``, so Next/Previous skip this stub.
