Features
========

Voxel features are the columns clustering sees: multi-sequence
intensities, texture families on one sequence, enhancement patterns
(ratio, difference, average, expression, kinetic), and concatenated
routes. See Examples → Features.
