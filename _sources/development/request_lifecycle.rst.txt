:orphan:

Complete Request Lifecycle (moved)
==================================

This page is no longer in the public Developer sidebar.

The two execution paths (v1 recipes vs v0.1 YAML translation) are
summarized on :doc:`architecture`.

The full original command walkthrough is archived in the repository at
``developer/sphinx_archive/request_lifecycle.rst``.
