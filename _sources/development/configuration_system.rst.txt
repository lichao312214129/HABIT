:orphan:

Configuration System (moved)
============================

This page is no longer in the public Developer sidebar.

v1 specs and the v0.1 YAML translation path are summarized on
:doc:`architecture`. Spec objects: :doc:`../api/spec`.

The full original YAML → schema → configurator notes are archived in the
repository at ``developer/sphinx_archive/configuration_system.rst``.
