:orphan:

Developer Guide
===============

This page is no longer in the sidebar. Public Developer documentation is
three pages; read them in this order:

1. :doc:`architecture` — layers, public API boundary, invariants.
2. :doc:`contributing` — environment, tests, and pull requests.
3. :doc:`../customization/index` — habitat registry and entry-point plugins.

Changelog and acknowledgments are top-level captions, not under Developer.
Third-party library notes are in :doc:`../reference/upstream_libraries`.

Former long-form pages (philosophy, mental model, YAML lifecycle, compat
engine tours, repo layout) remain as stubs at their old URLs. Full
originals are in the repository under ``developer/sphinx_archive/``.
