# Habitat Analysis Configuration

# Data paths
data_dir: F:\work\workstation_b\dingHuYingXiang\the_4_training_202504\demo_data\DATA_2
out_dir: F:\work\workstation_b\dingHuYingXiang\the_4_training_202504\demo_data

# Preprocessing settings
Preprocessing:
   
  # n4_correction:
  #   images: [pre_contrast, LAP, PVP, delay_3min]
  #   num_fitting_levels: 2

  # resample:
  #   images: [pre_contrast, LAP, PVP, delay_3min]
  #   target_spacing: [1.0, 1.0, 1.0]  # Target voxel spacing in x, y, z

  # # Z-Score标准化 - 将图像强度标准化为均值0、标准差1的分布
  # zscore_normalization:
  #   images: [pre_contrast, LAP, PVP, delay_3min]  # 要处理的图像序列
  #   only_inmask: false  # 是否仅在掩码内计算统计量
    # mask_key: mask  # 当only_inmask为true时使用的掩码名称

  # 直方图标准化 - 简化版  FIXME 调试版本
  # histogram_standardization:
  #   images: [pre_contrast, LAP, delay_3min]  # 要处理的图像序列
  #   reference_key: PVP  # 参考图像的键名

  # reorientation:
  #   images: [T2WI, ADC]
  #   target_orientation: LPS  # "LPS", "RAS", etc. Target anatomical orientation.
  #   mode: closest  # 'closest' (no interpolation) or 'strict' (with interpolation).
  #   # Masks mask_T2WI, mask_ADC are reoriented if present (nearest neighbor in strict), same as resample

  registration:
    images: [T2WI, ADC]
    fixed_image: T2WI
    # moving_images: not read; movers = images minus fixed_image
    type_of_transform: SyNRA  # support all ants registration methods
    use_mask: false
    # backend: simpleitk  # optional: ants (default) or simpleitk
    # SimpleITK-only (when backend: simpleitk): number_of_histogram_bins, metric_sampling_percentage,
    # shrink_factors_per_level, smoothing_sigmas_per_level, learning_rate, number_of_iterations,
    # bspline_mesh_size, bspline_order — see docs/source/user_guide/image_preprocessing_zh.rst

# General settings
processes: 1  # Number of parallel processes
random_state: 42  # Random seed for reproducibility
