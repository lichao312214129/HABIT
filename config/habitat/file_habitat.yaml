# =============================================================================
# 0. 文件说明 / Overview
# 中文：这是输入数据清单。带 ★ 的受试者 ID、模态名和路径必须替换为真实数据，且键名必须与主配置一致。
# EN: This is an input manifest. Replace ★ subject IDs, modality names and paths with real data, keeping keys consistent with the main config.
#
# 运行 / Run: habit get-habitat -c <配置文件.yaml>
#
# 【YAML 防错 / YAML safety】
#   - 仅用空格缩进，不用 Tab；冒号后必须有一个空格：key: value
#   - true / false / null 必须小写
#   - 路径通常无需引号；含空格或 # : * 等特殊字符时才使用双引号
#       D:/data/images 可以；"D:/my data/images" 必须加引号
#   - Windows 路径推荐使用正斜杠 /；相对路径以本 YAML 文件夹为起点
#   - 修改后先运行：habit check-config -c <本文件.yaml> --syntax-only（清单/参数预设仅做 YAML 语法检查）

# Habitat input manifest — DEMO: demo_data/preprocessed
# Use as `data_dir: file_habitat_demo.yaml` in habitat YAML under config/habitat/.
# Modality keys must match feature_construction (pre_contrast, LAP, PVP, delay_3min).
# Path convention: relative paths (../, ../../) resolve from this YAML file's directory,
# not the shell cwd. Example: ../../demo_data/... => repository demo_data/.
# 中文：同一受试者/模态有多个文件时是否自动选第一个；DICOM 文件夹输入通常应为 false。
# EN: Whether to select the first file when multiple exist per subject/modality; normally false for DICOM folders.
auto_select_first_file: true

# 中文：★【必须检查】影像模态或影像清单；模态键必须与实际文件夹及后续配置完全一致。
# EN: ★ REVIEW: Image modalities/manifest; modality keys must match folders and downstream config.
# =============================================================================

# =============================================================================
# 1. ★【必须修改 / MUST EDIT】数据、路径与运行入口
# EN: Data, paths and run entry
images:
  # 中文：★【必须修改】受试者 ID；替换为真实唯一 ID，并确保 images 与 masks 两处一致。
  # EN: ★ MUST EDIT: Subject ID; replace with a real unique ID and match images/masks.
  # --- 1.1 subj001 ---
  subj001:
    # 中文：★【必须修改】该受试者该模态的文件或文件夹路径；模态键须与主配置一致。
    # EN: ★ MUST EDIT: File/folder path for this subject and modality; keep the modality key consistent.
    pre_contrast: ../../demo_data/preprocessed/images/subj001/pre_contrast
    # 中文：★【必须修改】该受试者该模态的文件或文件夹路径；模态键须与主配置一致。
    # EN: ★ MUST EDIT: File/folder path for this subject and modality; keep the modality key consistent.
    LAP: ../../demo_data/preprocessed/images/subj001/LAP
    # 中文：★【必须修改】该受试者该模态的文件或文件夹路径；模态键须与主配置一致。
    # EN: ★ MUST EDIT: File/folder path for this subject and modality; keep the modality key consistent.
    PVP: ../../demo_data/preprocessed/images/subj001/PVP
    # 中文：★【必须修改】该受试者该模态的文件或文件夹路径；模态键须与主配置一致。
    # EN: ★ MUST EDIT: File/folder path for this subject and modality; keep the modality key consistent.
    delay_3min: ../../demo_data/preprocessed/images/subj001/delay_3min
  # 中文：★【必须修改】受试者 ID；替换为真实唯一 ID，并确保 images 与 masks 两处一致。
  # EN: ★ MUST EDIT: Subject ID; replace with a real unique ID and match images/masks.

  # --- 1.2 subj002 ---
  subj002:
    # 中文：★【必须修改】该受试者该模态的文件或文件夹路径；模态键须与主配置一致。
    # EN: ★ MUST EDIT: File/folder path for this subject and modality; keep the modality key consistent.
    pre_contrast: ../../demo_data/preprocessed/images/subj002/pre_contrast
    # 中文：★【必须修改】该受试者该模态的文件或文件夹路径；模态键须与主配置一致。
    # EN: ★ MUST EDIT: File/folder path for this subject and modality; keep the modality key consistent.
    LAP: ../../demo_data/preprocessed/images/subj002/LAP
    # 中文：★【必须修改】该受试者该模态的文件或文件夹路径；模态键须与主配置一致。
    # EN: ★ MUST EDIT: File/folder path for this subject and modality; keep the modality key consistent.
    PVP: ../../demo_data/preprocessed/images/subj002/PVP
    # 中文：★【必须修改】该受试者该模态的文件或文件夹路径；模态键须与主配置一致。
    # EN: ★ MUST EDIT: File/folder path for this subject and modality; keep the modality key consistent.
    delay_3min: ../../demo_data/preprocessed/images/subj002/delay_3min
# 中文：★【必须检查】分割 mask 清单；受试者和模态键应与 images 完全对应。
# EN: ★ REVIEW: Segmentation-mask manifest; subject and modality keys must match images exactly.
masks:
  # 中文：★【必须修改】受试者 ID；替换为真实唯一 ID，并确保 images 与 masks 两处一致。
  # EN: ★ MUST EDIT: Subject ID; replace with a real unique ID and match images/masks.

  # --- 1.3 subj001 ---
  subj001:
    # 中文：★【必须修改】该受试者该模态的文件或文件夹路径；模态键须与主配置一致。
    # EN: ★ MUST EDIT: File/folder path for this subject and modality; keep the modality key consistent.
    pre_contrast: ../../demo_data/preprocessed/masks/subj001/pre_contrast
    # 中文：★【必须修改】该受试者该模态的文件或文件夹路径；模态键须与主配置一致。
    # EN: ★ MUST EDIT: File/folder path for this subject and modality; keep the modality key consistent.
    LAP: ../../demo_data/preprocessed/masks/subj001/LAP
    # 中文：★【必须修改】该受试者该模态的文件或文件夹路径；模态键须与主配置一致。
    # EN: ★ MUST EDIT: File/folder path for this subject and modality; keep the modality key consistent.
    PVP: ../../demo_data/preprocessed/masks/subj001/PVP
    # 中文：★【必须修改】该受试者该模态的文件或文件夹路径；模态键须与主配置一致。
    # EN: ★ MUST EDIT: File/folder path for this subject and modality; keep the modality key consistent.
    delay_3min: ../../demo_data/preprocessed/masks/subj001/delay_3min
  # 中文：★【必须修改】受试者 ID；替换为真实唯一 ID，并确保 images 与 masks 两处一致。
  # EN: ★ MUST EDIT: Subject ID; replace with a real unique ID and match images/masks.

  # --- 1.4 subj002 ---
  subj002:
    # 中文：★【必须修改】该受试者该模态的文件或文件夹路径；模态键须与主配置一致。
    # EN: ★ MUST EDIT: File/folder path for this subject and modality; keep the modality key consistent.
    pre_contrast: ../../demo_data/preprocessed/masks/subj002/pre_contrast
    # 中文：★【必须修改】该受试者该模态的文件或文件夹路径；模态键须与主配置一致。
    # EN: ★ MUST EDIT: File/folder path for this subject and modality; keep the modality key consistent.
    LAP: ../../demo_data/preprocessed/masks/subj002/LAP
    # 中文：★【必须修改】该受试者该模态的文件或文件夹路径；模态键须与主配置一致。
    # EN: ★ MUST EDIT: File/folder path for this subject and modality; keep the modality key consistent.
    PVP: ../../demo_data/preprocessed/masks/subj002/PVP
    # 中文：★【必须修改】该受试者该模态的文件或文件夹路径；模态键须与主配置一致。
    # EN: ★ MUST EDIT: File/folder path for this subject and modality; keep the modality key consistent.
    delay_3min: ../../demo_data/preprocessed/masks/subj002/delay_3min
# =============================================================================
