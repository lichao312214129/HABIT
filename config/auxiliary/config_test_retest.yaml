# Path convention: relative paths (../, ../../) resolve from this YAML file's directory,
# not the shell cwd. Example: ../../demo_data/... => repository demo_data/.
# Path convention: relative paths (../, ../../) resolve from this YAML file's directory,
# not the shell cwd. Example: ../../demo_data/... => repository demo_data/.

# Configuration file for Test-Retest Reproducibility Analysis
# This configuration is used by habit retest command

# Output directory for remapped NRRD files and logs
out_dir: ../../demo_data/ml_data/test_retest/remapped

# Test habitat table (from resample_02 two-step habitat train output)
test_habitat_table: ../../demo_data/results/habitat_two_step/habitats.csv

# Retest habitat table (demo: same table for label-mapping smoke test)
retest_habitat_table: ../../demo_data/ml_data/habitats_retest.csv

# Features to use for similarity calculation
# If not specified, all features will be used
features: null  # null means use all features
# Example: features: ['feature1', 'feature2', 'feature3']

# Similarity method for habitat mapping
# Options: 'pearson', 'spearman', 'kendall', 'euclidean', 'cosine', 'manhattan', 'chebyshev'
similarity_method: pearson

# Input directory containing retest NRRD files
input_dir: ../../demo_data/habitat_maps/retest

# Number of parallel processes to use
processes: 4

# Enable debug logging
debug: false
