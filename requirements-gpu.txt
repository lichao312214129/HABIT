# HABIT optional GPU PyTorch (CUDA 12.1, Python 3.10).
# Large download — use only when you have an NVIDIA GPU and want TorchRadiomics GPU acceleration.
#
# Install base dependencies first (includes CPU torch):
#   pip install -r requirements.txt
#   pip install -e .
# Then replace CPU torch with the CUDA build:
#   pip install -r requirements-gpu.txt --upgrade --force-reinstall
#
# Verify: python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
# Expect version containing +cu121 and CUDA available True.

--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.4.0+cu121
