# Pinned for Python 3.10 compatibility (tested in py310 conda env).
# Does NOT include PyTorch (large download). After this file, install ONE of:
#   pip install -r requirements-cpu.txt   # recommended for most users
#   pip install -r requirements-gpu.txt   # NVIDIA GPU + CUDA 12.1 only
numpy==1.26.1
SimpleITK==2.2.1
antspyx==0.4.2
opencv-python
matplotlib
trimesh
scipy
openpyxl
pandas
tqdm
# pyradiomics: not listed here (avoid Windows source build / MSVC).
#   Windows py310: pip install pyradiomics-3.0.1-cp310-cp310-win_amd64.whl  (wheel in project root)
#   macOS / Linux: pip install pyradiomics  (before or after this file; see installation_zh.rst)
scikit-learn
scikit-image
mrmr_selection
pingouin
statsmodels
xgboost
seaborn
shap
pyyaml
click>=8.0
chardet
lifelines
pydicom
pydantic
kneed