# Pinned for Python 3.10 compatibility (tested in py310 conda env)
numpy==1.26.1
SimpleITK==2.2.1
antspyx==0.4.2
opencv-python
matplotlib
trimesh
scipy
openpyxl
pandas
tqdm
# Build-time helper for pyradiomics sdist (bundled versioneer.py); install before pyradiomics if build fails
versioneer>=0.29
pyradiomics
scikit-learn
scikit-image
mrmr_selection
pingouin
statsmodels
xgboost
seaborn
shap
pyyaml
click>=8.0
chardet
lifelines
pydicom
pydantic
kneed

# PyTorch CPU (default; smaller/faster download). HABIT runs on CPU torch.
# NVIDIA GPU + CUDA 12.1: after this file, run: pip install -r requirements-gpu.txt
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.0